[project]
name = "git-correlation-agent"
version = "0.1.0"
description = "AI Agent for Git-Kiro semantic correlation analysis"
requires-python = ">=3.12"
dependencies = [
    "strands-agents==1.38.0",
    "boto3==1.43.4",
]

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"
